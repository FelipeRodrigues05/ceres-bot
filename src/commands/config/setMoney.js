const { MessageEmbed } = require("discord.js")
const { QuickDB } = require("quick.db")
const db = new QuickDB()

module.exports = {
  name: "setMoney",
  aliases: ['setmoney', "setdinheiro"],

  run: async (discordClient, message, args) => {
    
  }
}